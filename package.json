{
  "name": "vili-micro-frontend-template",
  "displayName": "vili react typescript template",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "serve": "turbo run serve --parallel"
  },
  "devDependencies": {
    "prettier": "^2.6.2",
    "turbo": "^1.2.9"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=14.0.0"
  },
  "packageManager": "npm@8.3.1"
}
